version: '3.9'

services:
  postgres:
    image: postgres:16
    container_name: user-management-postgres
    
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ot_P2tHj
      POSTGRES_DB: user_management
    
    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

